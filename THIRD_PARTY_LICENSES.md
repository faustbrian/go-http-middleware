# Third-party attribution

| Module | Use | License |
|---|---|---|
| `github.com/felixge/httpsnoop` | exact `ResponseWriter` interface wrapping | MIT |
| `golang.org/x/net` | IDNA lookup profile | BSD-3-Clause |
| `golang.org/x/text` | transitive Unicode tables | BSD-3-Clause |

Release preparation runs `go list -m all` and vulnerability scanning. A release
maintainer must update this table when the module graph changes.
